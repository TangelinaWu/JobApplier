{
  "name": "jobapplier",
  "version": "1.0.0",
  "description": "Desktop app that launches Chromium with a pre-loaded job application auto-filler extension",
  "main": "electron-main.js",
  "scripts": {
    "start": "electron ."
  },
  "devDependencies": {
    "electron": "^42.3.3"
  }
}
